apiVersion: v1
kind: Service
metadata:
  name: flask-autoscaling-service
  namespace: autoscaling-demo
  labels:
    app: flask-autoscaling-app
spec:
  type: ClusterIP
  selector:
    app: flask-autoscaling-app
  ports:
    - name: http
      port: 80
      targetPort: http
