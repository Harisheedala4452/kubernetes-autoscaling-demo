apiVersion: v1
kind: ConfigMap
metadata:
  name: flask-autoscaling-config
  namespace: autoscaling-demo
data:
  # Beginners can change this value to simulate a new application version.
  APP_VERSION: "1.0.0"
  FLASK_ENV: "production"
